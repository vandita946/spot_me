<%= @connections %>



julien's connections = joel, vandita
julien's buddies = ""
julien's buddy of = ""
julien's followers = claire, vandita

<!-- Connection.new(owner: julien, buddy: joel).save
Connection.new(owner: julien, buddy: vandita).save
Connection.new(owner: claire, buddy: julien).save
Connection.new(owner: vandita, buddy: julien).save
Connection.new(owner: joel, buddy: claire).save
 -->


<ul class="nav nav-tabs">
  <li class="nav-item">
    <a href="#connections" data-toggle="tab" role="tab" class="nav-link active">Connections</a>
  </li>
  <li class="nav-item">
    <a href="#buddies" data-toggle="tab" role="tab" class="nav-link">Buddies</a>
  </li>
  <li class="nav-item">
    <a href="#buddyof" data-toggle="tab" role="tab" class="nav-link">Buddy of..</a>
  </li>
  <li class="nav-item">
    <a href="#connectionof" data-toggle="tab" role="tab" class="nav-link">Connection of...</a>
  </li>

<div class="tab-content">
  <div role="tab-panel" class="tab-pane active" id="connections">
    <div class="cards">
    <% @connections.each do |user| %>
      <li><%= user.buddy.firstname %> <%= user.buddy.lastname %></li>
    <% end %>
    </div>
  </div>

  <div role="tab-panel" class="tab-pane" id="buddies">
    <div class="cards">

    </div>
  </div>

  <div role="tab-panel" class="tab-pane" id="buddyof">
    <div class="cards">
      <% @connections.each do |user| %>
        <li><%= "tbd" %></li>
      <% end %>

    </div>
  </div>

  <div role="tab-panel" class="tab-pane" id="connectionof">
     <div class="cards">
      <% @connections.each do |user| %>
        <li><%= "tbd" %></li>
      <% end %>

    </div>
  </div>

  <% raise %>
